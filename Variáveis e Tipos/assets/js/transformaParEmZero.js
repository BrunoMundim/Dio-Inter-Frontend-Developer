/* function pairToZero(array){
    if (!array) return -1;
    if (!array.length) return -1;

    for(var i = 0; i <= array.length; i++){
        if(array[i] % 2 == 0){
            array.splice(i, 1, 0); // ou array[i] = 0;
        }        
    }
}

console.log(pairToZero(false)); */
const meuObjeto = {
    chave: 'valor'
   }
   
   console.log(meuObjeto.chave); 

   var string = 'abc';
    console.log(string.length); 

